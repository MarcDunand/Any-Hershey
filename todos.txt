Project updates:
-Recreate venv on this computer (the copied one has no python.exe; venvs don't carry over between machines)
	-python -m venv venv; .\venv\Scripts\Activate.ps1; pip install -r requirements-win.txt
-Git cleanup for the fixed .gitignore
	-git rm -r --cached venv build dist
	-git rm txt2svg.spec
	-git add -A

Once updates are done:
-Rebuild with .\build_win.ps1 and test AnyHershey.exe (missing-font warning, Latin preset uses Arial)
-New GitHub release: upload dist\AnyHershey_windows.zip, release notes using the AnyHershey name (v0.1 notes and exe still say txt2svg)
-Update README.md: retake docs/ui.png (window title is now AnyHershey), check the release link and any version references, reflect any preset changes


Useful links:
	-https://wordscr.com/about/
